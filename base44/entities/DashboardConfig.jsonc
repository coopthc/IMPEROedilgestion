{
  "name": "DashboardConfig",
  "type": "object",
  "properties": {
    "widgets": {
      "type": "string",
      "title": "Widget ordinati (JSON array di tipi)"
    },
    "quick_actions": {
      "type": "string",
      "title": "Azioni rapide visibili (JSON array di tipi)"
    }
  },
  "required": [],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}