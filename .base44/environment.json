{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "Base44 app ID identifying the app on the hosted Base44 backend (EdilGestion).",
      "requiredAtBoot": false
    },
    {
      "name": "VITE_BASE44_APP_BASE_URL",
      "description": "Deployed Base44 app URL the local frontend proxies /api requests to (e.g. https://edilgestion.base44.app).",
      "requiredAtBoot": false
    }
  ]
}
